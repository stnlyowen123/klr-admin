/** @type {import('next').NextConfig} */
const config = {
    pageExtensions: ["ts", "tsx", "js", "jsx"], // Mendukung semua ekstensi halaman
  
    // Aktifkan output standalone untuk build
    output: "standalone",
  };
  
  export default config;
  